import { makeStyles } from 'tss-react/mui';

const useStyles = makeStyles()((theme) => ({
  label: {
    marginTop: '1rem',
    marginBottom: 0,
    fontWeight: 'bold',
  },
}));
export default useStyles;
